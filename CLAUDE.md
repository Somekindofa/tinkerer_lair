# Claude Code entry point

This project's guide for LLM sessions — protocol, taste, known bug
classes, and a "where to look for a reference implementation" map — lives
in [AGENTS.md](./AGENTS.md). Read that file; this one exists only because
Claude Code specifically looks for `CLAUDE.md`, and keeping the content in
one place avoids the two drifting apart.
